import React from 'react';
import { ScrollView, Text, View } from 'react-native';

const Profile = () => (
  <ScrollView>
    <View>
      <Text>This is a dummy Profile screen.</Text>
    </View>
  </ScrollView>
);

export default Profile;
