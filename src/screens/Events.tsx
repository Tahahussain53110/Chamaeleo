import React from 'react';
import { ScrollView, Text, View } from 'react-native';

const Events = () => (
  <ScrollView>
    <View>
      <Text>This is a dummy Events screen.</Text>
    </View>
  </ScrollView>
);

export default Events;
