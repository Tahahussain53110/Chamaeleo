import React from 'react';
import { ScrollView, Text, View } from 'react-native';

const Bookings = () => (
  <ScrollView>
    <View>
      <Text>This is a dummy Bookings screen.</Text>
    </View>
  </ScrollView>
);

export default Bookings;
