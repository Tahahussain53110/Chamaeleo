import React from 'react';
import { ScrollView, Text, View } from 'react-native';

const Notification = () => (
  <ScrollView>
    <View>
      <Text>This is a dummy Notification screen.</Text>
    </View>
  </ScrollView>
);

export default Notification;
